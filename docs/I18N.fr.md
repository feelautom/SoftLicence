# 🌐 Internationalisation & Ressources Locales (I18N)

SoftLicence supporte la gestion multi-langue (actuellement **Anglais** et **Français**) et est conçu pour fonctionner dans des environnements industriels restreints sans accès Internet.

## 🛠️ Configuration

La localisation côté serveur est gérée par `IStringLocalizer` et des fichiers de ressources (`.resx`) situés dans le dossier `Resources/`.

- **Langue par défaut** : Anglais (`en`).
- **Langues supportées** : Anglais (`en`), Français (`fr`).

Pour changer la langue par défaut, modifiez le fichier `Program.cs` :
```csharp
app.UseRequestLocalization(options => {
    options.SetDefaultCulture("en"); // Changez en "fr" pour le français
    // ...
});
```

## 📦 Ressources Locales (Mode Hors-Ligne)

Toutes les dépendances de l'interface utilisateur sont hébergées localement dans `wwwroot/lib/` pour garantir que la console d'administration fonctionne sans CDN ou connexion Internet.

### Bootstrap
- **CSS** : `wwwroot/lib/bootstrap/css/bootstrap.min.css`
- **JS** : `wwwroot/lib/bootstrap/js/bootstrap.bundle.min.js`

### Icônes de Drapeaux
Les drapeaux sont gérés via la bibliothèque CSS **flag-icons** (chargée via CDN dans `App.razor`). Ils sont utilisés dans le sélecteur de langue et le journal d'audit.

## 🚩 Ajouter une nouvelle langue

Si vous souhaitez ajouter une nouvelle langue (ex: Espagnol - `es`) :

### 1. Fichiers de ressources
1. Créez une copie de `SharedResource.en.resx` et renommez-la en `SharedResource.es.resx`.
2. Traduisez toutes les balises `<value>` à l'intérieur du XML.

### 2. Mettre à jour l'UI
Dans `src/SoftLicence.Server/Components/Layout/MainLayout.razor`, ajoutez la nouvelle langue au menu déroulant en utilisant la classe CSS appropriée (`fi fi-es`) :
```html
<li>
    <a class="dropdown-item d-flex align-items-center gap-2" href="/Culture/Set?culture=es&redirectUri=@System.Net.WebUtility.UrlEncode(Nav.Uri)">
        <span class="fi fi-es"></span> Español
    </a>
</li>
```

### 4. Activer dans Program.cs
Ajoutez le code de la culture à la liste des cultures supportées dans `Program.cs` :
```csharp
var supportedCultures = new[] { "en", "fr", "es" };
```
