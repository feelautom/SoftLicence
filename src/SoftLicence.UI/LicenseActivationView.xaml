<UserControl x:Class="SoftLicence.UI.LicenseActivationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Margin="20">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="Gestion de Licence" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <TextBlock Text="Hardware ID (à fournir au support) :" Foreground="Gray"/>
            <TextBox Text="{Binding CurrentHardwareId}" IsReadOnly="True" Background="#EEE" Padding="5" Margin="0,0,0,10"/>

            <TextBlock Text="Clé de licence :" Margin="0,10,0,5"/>
            <TextBox Text="{Binding LicenseKey, UpdateSourceTrigger=PropertyChanged}" 
                     Height="100" TextWrapping="Wrap" AcceptsReturn="True" 
                     VerticalScrollBarVisibility="Auto" Padding="5"/>

            <Button Content="Activer / Vérifier" Command="{Binding ActivateCommand}" 
                    Margin="0,10,0,10" Padding="10,5" Background="#007ACC" Foreground="White" FontWeight="Bold"/>

            <TextBlock Text="{Binding StatusMessage}" Foreground="Red" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,0,0,10"/>

            <Border BorderBrush="Green" BorderThickness="1" CornerRadius="5" Padding="10" 
                    Visibility="{Binding IsLicensed, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel>
                    <TextBlock Text="LICENCE VALIDE" Foreground="Green" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding LicenseInfo}" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
